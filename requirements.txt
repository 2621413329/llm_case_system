# LLM Case System — Python 依赖（pip install -r requirements.txt）
# 建议：Python 3.10+，使用 venv 隔离环境。
# 说明：sentence-transformers 会拉取 torch 等，磁盘与内存占用较大；不需要向量能力时可暂不装该行（需自行处理 import 失败路径）。

# ---------- 配置加载 ----------
PyYAML>=6.0.0

# ---------- MySQL 持久化（二选一即可；代码优先 pymysql）----------
pymysql>=1.0.0
mysql-connector-python>=8.0.0

# ---------- 向量 / 检索 / 可视化（需求网络、向量搜索、降维等）----------
numpy>=1.24.0
scikit-learn>=1.3.0
sentence-transformers>=2.2.0

# ---------- 本地 OCR（可选；需系统安装 Tesseract 可执行文件）----------
pytesseract>=0.3.10
Pillow>=9.0.0

# ---------- 测试与脚本 ----------
pytest>=7.0.0
requests>=2.28.0
